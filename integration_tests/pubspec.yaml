name: solarpro_integration_tests
description: Integration test infrastructure for Solar Pro Supabase RLS rules.
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  supabase: ^2.13.0

dev_dependencies:
  test: ^1.25.0
